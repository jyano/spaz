
    //$a.u($e.bodyParser({uploadDir:__dirname+"/public/uploads",keepExtensions:true}) ) //$a.u($e.cookieParser('xyz'))


    //$mg= mongoose = require('mongoose')

    //$l('env: ' + $a.get('env'))
    //$a.use( $e.favicon() ) //$w = middleware =require('./middleware')

//$a.use(middleware)

//$a.use( $e.logger('dev') )
//if(a.get('env')=="development"){a.use($e.errorHandler())}

    /*
     $a.use($e.session({
     store: mongoStore,
     secret: 'xyz' }))
     */
//mongoose.connect("mongodb://localhost/brain", function(){$l('mongo connected')})
//models = $m = require('./models')
// require('./mongodb') // raw mongo
//mongoStore = new(connectMongo($e))({db:'brain'})

    //io = socketIO.listen(httpServer) //io.set('log level', 1) //require('./sockets')(  io,   new sessionSocketIO(io, mongoStore, cookieParser) )

